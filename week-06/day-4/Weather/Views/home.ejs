<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <link href="https://fonts.googleapis.com/css?family=Lato:400,900" rel="stylesheet">
  <title>Document</title>
</head>
<body>
  <div id="container">
    <% for ( let i=0;i < forecasts.length;i++){ %>
    <div id="dash-<%= i %>" class="dashboard" onclick=<% click(forecasts,i) %>
      <div class="location">
        <h1><%= forecasts[i].city %></h1>
        <p><%= forecasts[i].location %></p>
      </div>
      <div class="temp">
        <p><%= forecasts[i].weather[1].temp %>°C</p>
      </div>
      <div class="icon">
        <img src="/images/<%= forecasts[i].weather[1].icon %>.png" alt="">
      </div>
    </div>
    <% } %>
  </div>
  <script src="../index.js"></script>
</body>
</html>